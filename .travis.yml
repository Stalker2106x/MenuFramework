language: cpp

compiler:
  - gcc
  - clang

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-6
      - g++-6
      - cmake

before_script:
  - export CC=gcc-6
  - export CXX=g++-6
  - mkdir build
  - cd build
  - cmake ..

script:
  - make